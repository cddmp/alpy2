# alpy2

Just a Dockerfile which allows to spawn a Docker container for python2 experiments.

## Usage
```
git clone https://github.com/cddmp/alpy2
docker build alpy2 --tag alpy2
docker run -v /home/you/whatever/you/want:/root -it alpy2
```
